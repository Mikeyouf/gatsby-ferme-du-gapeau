import React from 'react'
import styled from 'styled-components'

const NavApp = styled.nav`
    width: 100%;
    color: #fff;
    align-self: center;
`

const Navigation = () => (
    <NavApp>
        test
    </NavApp>
)

export default Navigation